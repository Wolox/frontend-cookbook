import { StringObject } from '@interfaces/globalInterfaces';

const Routes: StringObject = {
  Auth: 'Auth',
  Login: 'Login',
  SignUp: 'SignUp',
  App: 'App',
  Home: 'Home'
};

export default Routes;
