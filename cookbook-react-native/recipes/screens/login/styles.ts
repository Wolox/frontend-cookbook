import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
    width: '100%'
  },
  form: {
    width: 250
  },
  formButton: {
    padding: 10,
    borderRadius: 3,
    marginTop: 15
  }
});
