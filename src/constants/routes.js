const Routes = {
  DASHBOARD: '/',
  PRESENTATION: '/present',
  CATEGORY: '/:category',
  DETAIL: '/:category/:component',
  LOGIN: '/login',
  RECOVER_PASSWORD: '/forgot_password'
};

export default Routes;
